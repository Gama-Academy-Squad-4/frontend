import styled from 'styled-components';

export const P = styled.p`
  margin: 0;
  font-size: 12px;
  margin-bottom: 5px;
  color: #0a0a0a;
  font-family: Helvetica, Sans-Serif;
  font-size: 0.9rem;
  font-weight: 600;
`;
